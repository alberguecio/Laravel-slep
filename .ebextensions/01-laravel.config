option_settings:
  aws:elasticbeanstalk:application:environment:
    APP_ENV: production
    APP_DEBUG: false
    LOG_CHANNEL: errorlog

# container_commands comentados temporalmente
# Se ejecutarán manualmente después del despliegue exitoso
# container_commands:
#   01_create_storage:
#     command: "mkdir -p storage/app/public storage/framework/{sessions,views,cache} storage/logs"
#     ignoreErrors: true
#   02_permissions:
#     command: "chmod -R 775 storage bootstrap/cache || true"
#     ignoreErrors: true
#   03_storage_link:
#     command: "php artisan storage:link || true"
#     ignoreErrors: true

